- name: Frontend setup
  hosts: all
  become: yes
  tasks:
    - name: Install a modularity appstream with defined stream
      ansible.builtin.dnf:
        name: '@nginx:1.26'
        state: present

